import StoryImage from '../../../components/StoryImage'
import HeroImage from '../../../components/HeroImage'
import StoryMeta from '../../../components/StoryMeta'

# The time ryan fixed the truck

<StoryMeta author="Julie Richings" />

The time ryan fixed the truckThe time ryan fixed the truckThe time ryan fixed the truckThe time ryan fixed the truck
